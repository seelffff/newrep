# (Binance ↔️ MEXC Futures)

Арбитражный бот для автоматического детектирования возможностей арбитража между Binance Futures и MEXC Futures в реальном времени через WebSocket.

### Мониторинг и детектирование
- **WebSocket в реальном времени** — мгновенное получение цен с обеих бирж
- **Автоматическое детектирование арбитража** — находит разницу цен между биржами
- **Учет комиссий** — вычисляет реальную прибыль после всех комиссий
- **Топ пар** — мониторит самые ликвидные торговые пары
- **Автопереподключение** — восстанавливает соединение при обрыве



### Отчетность и аналитика
- **Excel отчеты** — автоматическая генерация детальных отчетов
- **Мониторинг WebSocket** — отслеживание downtime и uptime
- **Детальное логирование** — полная информация о каждой позиции
- **4 листа в отчете** — Сводка, Сделки, WebSocket Downtime, Детальная статистика

### Гибкость
- **Гибкая настройка** — конфиг для всех параметров торговли
- **Cross/Isolated margin** — поддержка обоих режимов маржи
- **Настраиваемое плечо** — от 1x до 125x

## Требования

- Node.js 18+
- npm или yarn

## 🛠️ Установка

```bash
# 1. Клонировать репозиторий
git clone https://github.com/seelffff/newrep.git
cd newrep

# 2. Установить зависимости
npm install

# 3. Создать .env файл (опционально - для торговли нужны API ключи)
cp .env.example .env

# 4. Настроить config.json (по желанию)
# По умолчанию готов к работе!
```

## ⚙️ Настройка

### config.json

Основные параметры в [config.json](config.json):

```json
{
  "arbitrage": {
    "minSpreadPercent": 0.65,      // Минимальный спред для детектирования
    "topPairsCount": 50,            // Количество топ пар для мониторинга
    "useWebSocket": true,           // ОБЯЗАТЕЛЬНО true для арбитража!
    "restFallbackIntervalMs": 0,    // 0 = не использовать REST fallback
    "reconnectDelayMs": 3000        // Задержка перед переподключением
  },

  "notifications": {
    "logToConsole": true,
    "minSpreadToNotify": 1.0,       // Показывать только спреды >= 1%
    "coloredOutput": true
  },

  "fees": {
    "binance": {
      "maker": 0.02,                // Проверь свой VIP уровень!
      "taker": 0.04
    },
    "mexc": {
      "maker": 0.0,
      "taker": 0.03
    }
  }
}
```

### .env (опционально)

API ключи нужны **только для торговли**. Для мониторинга цен они не требуются!

```env
BINANCE_API_KEY=your_key_here
BINANCE_API_SECRET=your_secret_here

MEXC_API_KEY=your_key_here
MEXC_API_SECRET=your_secret_here
```

## Запуск

### Режим разработки

```bash
npm run dev
```

Автоматическая перезагрузка при изменении кода.

### Production сборка

```bash
# Собрать проект
npm run build:prod

# Запустить
npm start
```

### Проверка типов

```bash
npm run typecheck
```

## 📊 Пример вывода

```
ЗАПУСК АРБИТРАЖНОГО БОТА
════════════════════════════════════════

✓ Binance API доступен
✓ MEXC API доступен
✓ Найдено 25 общих пар для мониторинга

Топ-10 отслеживаемых пар:
  1. BTCUSDT
  2. ETHUSDT
  3. SOLUSDT
  ...

🔍 Ожидание арбитражных возможностей...

23:48:04 | RIVERUSDT | Купить: BINANCE @ 24.0630 | Продать: MEXC @ 24.3610 | Спред: 1.24% | Прибыль: 1.17%
23:48:46 | RIVERUSDT | Купить: BINANCE @ 24.0620 | Продать: MEXC @ 24.3890 | Спред: 1.36% | Прибыль: 1.29%
```

## Как это работает

### 1. Получение топ пар
Бот запрашивает топ 50 пар по объему торгов с обеих бирж и находит **общие** пары.

### 2. WebSocket подключение
Подключается к WebSocket обеих бирж и подписывается на `bookTicker` (лучшие bid/ask цены) в реальном времени.

### 3. Детектирование арбитража
При каждом обновлении цены:
- Сравнивает **ask** (цена продажи) одной биржи с **bid** (цена покупки) другой
- Вычисляет спред: `(sellPrice - buyPrice) / buyPrice * 100`
- Проверяет минимальный порог (`minSpreadPercent`)

### 4. Учет комиссий
Вычитает комиссии обеих бирж:
```
buyPriceWithFee = buyPrice * (1 + takerFee)
sellPriceWithFee = sellPrice * (1 - takerFee)
profitPercent = (sellPriceWithFee - buyPriceWithFee) / buyPriceWithFee * 100
```

### 5. Уведомление
Если спред >= `minSpreadToNotify`, выводит цветное уведомление.

## Структура проекта

```
newrep/
├── src/
│   ├── types/
│   │   ├── config.ts              # Типы конфигурации
│   │   └── exchange.ts            # Типы данных бирж и позиций
│   ├── utils/
│   │   ├── config-loader.ts       # Загрузка и валидация конфига
│   │   ├── logger.ts              # Цветной вывод в консоль
│   │   ├── websocket-monitor.ts   # Мониторинг WebSocket соединений
│   │   └── excel-reporter.ts      # Генерация Excel отчетов
│   ├── exchanges/
│   │   ├── binance-futures.ts     # Класс для Binance Futures
│   │   └── mexc-futures.ts        # Класс для MEXC Futures
│   ├── arbitrage-detector.ts      # Логика детектирования арбитража
│   ├── trade-executor.ts          # Управление торговыми позициями
│   └── main.ts                    # Точка входа
├── reports/                       # Директория для Excel отчетов
├── config.json                    # Настройки приложения
├── .env.example                   # Пример .env файла
├── README.md                      # Основная документация
├── EXCEL_REPORTS.md               # Руководство по Excel отчетам
├── package.json
└── tsconfig.json
```

## Расширенная настройка

### Изменить количество отслеживаемых пар

В [config.json](config.json):
```json
"topPairsCount": 100  // Увеличить до 100 пар
```

**Внимание**: Binance WebSocket ограничен 1024 одновременными подписками.

### Исключить определенные пары

```json
"excludePairs": ["BTCUSDT", "ETHUSDT"]
```

### Изменить минимальный спред

```json
"minSpreadPercent": 0.5  // Более агрессивный поиск
```

**Важно**: Помни про комиссии! Для Binance + MEXC минимальная прибыль ~0.07% (0.04% + 0.03%).

### Отключить REST fallback

```json
"restFallbackIntervalMs": 0
```

При обрыве WebSocket бот остановится (рекомендуется для арбитража).

## ⚠️ Важные замечания

### Для успешного арбитража:

1. **WebSocket обязателен** — REST API слишком медленный
2. **Учитывай комиссии** — проверь свой VIP уровень на обеих биржах
3. **Проскальзывание** — реальная цена может отличаться при исполнении ордера
4. **Ликвидность** — большие объемы требуют достаточной глубины рынка
5. **Скорость вывода** — время перевода средств между биржами может быть долгим


## Безопасность

- Используй API ключи **только для чтения** для мониторинга
- Для торговли создавай отдельные ключи с ограниченными правами
- Включи IP whitelist на биржах

## Документация

### Внутренняя документация
- [EXCEL_REPORTS.md](EXCEL_REPORTS.md) — Руководство по Excel отчетам

### Документация API бирж
- [Binance Futures API](https://developers.binance.com/docs/derivatives/usds-margined-futures)
- [MEXC Futures API](https://mexcdevelop.github.io/apidocs/contract_v1_en/)



## 📄 Лицензия

ISC

## ⚡ Быстрый старт

```bash
npm install && npm run dev
```


---

**Отказ от ответственности**: Этот бот создан в образовательных целях. Торговля криптовалютой несет высокие риски. Автор не несет ответственности за финансовые потери.
