# 📖 Руководство по торговому модулю

## 🎯 Обзор

Арбитражный бот теперь поддерживает **автоматическое открытие и закрытие парных позиций** (LONG + SHORT) при обнаружении спредов между биржами.

### Два режима работы:

1. **Мониторинг** (`trading.enabled: false`) — только показывает спреды
2. **Торговля** (`trading.enabled: true`) — автоматически открывает позиции

В торговом режиме доступны:
- **Тестовый режим** (`testMode: true`) — имитация без реальных ордеров
- **Реальная торговля** (`testMode: false`) — реальные деньги ⚠️

---

## ⚙️ Настройка

### config.json

```json
{
  "trading": {
    "enabled": true,
    "_enabled": "Включить/выключить автоматическое открытие позиций",

    "testMode": true,
    "_testMode": "true = имитация, false = реальные деньги",

    "positionSizeUSD": 100,
    "_positionSizeUSD": "Размер одной позиции в USD (на каждую биржу)",

    "marginMode": "cross",
    "_marginMode": "'cross' (кросс-маржа) или 'isolated' (изолированная)",

    "leverage": 1,
    "_leverage": "Плечо (1-125). Для арбитража рекомендуется 1-3",

    "maxOpenPositions": 3,
    "_maxOpenPositions": "Максимум одновременно открытых пар позиций",

    "positionTimeoutSeconds": 3600,
    "_positionTimeoutSeconds": "Максимальное время жизни позиции (секунды)",

    "closeOnSpreadConvergence": true,
    "_closeOnSpreadConvergence": "Закрывать при схождении спреда",

    "minProfitToClosePercent": 0.3
    "_minProfitToClosePercent": "Минимальная прибыль % для закрытия"
  }
}
```

---

## 🔄 Как работает торговля

### 1. Обнаружение арбитража

Когда бот находит спред >= `minSpreadPercent`:

```
Обнаружен спред 1.36%
BTCUSDT: Binance @ 42,000 | MEXC @ 42,570
Прибыль после комиссий: 1.29%
```

### 2. Открытие парных позиций

Автоматически открываются **две позиции**:

**LONG позиция** (покупаем на дешевой бирже):
- Биржа: Binance
- Сторона: LONG
- Цена входа: 42,000
- Размер: $100 USD

**SHORT позиция** (продаем на дорогой бирже):
- Биржа: MEXC
- Сторона: SHORT
- Цена входа: 42,570
- Размер: $100 USD

```
✓ Открыта пара позиций #1
  Symbol: BTCUSDT
  LONG:  BINANCE @ 42000.00
  SHORT: MEXC @ 42570.00
  Спред: 1.36%
  Ожидаемая прибыль: 1.29%
  Закрытие через: 3600s
```

### 3. Мониторинг позиций

Бот постоянно отслеживает:
- **Текущий спред** между биржами
- **Время жизни** позиции
- **Условия закрытия**

### 4. Закрытие позиций

Позиции закрываются в двух случаях:

#### A. Схождение спреда (если `closeOnSpreadConvergence: true`)

Когда:
1. Спред сошелся < `minSpreadPercent`
2. И прибыль >= `minProfitToClosePercent`

```
Спред сошелся для BTCUSDT, прибыль 0.85% - закрываем...

🔒 Закрыта пара позиций (Схождение спреда)
  Symbol: BTCUSDT
  Время удержания: 120s
  Прибыль: 0.85% ($1.70)
  Открытых позиций: 0
```

#### B. Таймаут

Когда истекло `positionTimeoutSeconds` (по умолчанию 3600s = 1 час):

```
⏰ Таймаут для BTCUSDT (3600s истекло)

🔒 Закрыта пара позиций (Таймаут)
  Symbol: BTCUSDT
  Время удержания: 3600s
  Прибыль: 0.12% ($0.24)
  Открытых позиций: 0
```

---

## 📊 Расчет прибыли

### Формула:

```
Прибыль LONG = (цена_выхода - цена_входа) / цена_входа * 100
Прибыль SHORT = (цена_входа - цена_выхода) / цена_входа * 100
Общая прибыль = (LONG + SHORT) / 2
```

### Пример:

**Открытие:**
- LONG: вход @ 42,000
- SHORT: вход @ 42,570
- Спред: 1.36%

**Закрытие:**
- LONG: выход @ 42,300 (+0.71%)
- SHORT: выход @ 42,400 (+0.40%)
- **Итого: +0.56% прибыли**

**В USD:**
- LONG: +$0.71
- SHORT: +$0.40
- **Итого: +$1.11 (на $200 капитала)**

---

## 🎮 Тестовый режим

### Как включить:

```json
{
  "trading": {
    "enabled": true,   // Включена торговля
    "testMode": true   // ТЕСТОВЫЙ режим
  }
}
```

### Что происходит:

✅ Бот **имитирует** все действия:
- Открытие позиций
- Мониторинг спреда
- Закрытие позиций
- Расчет PnL

✅ Выводит **подробные логи**:

```
🧪 [TEST] Имитация открытия позиций для BTCUSDT...
✓ Открыта пара позиций #1
  Symbol: BTCUSDT
  ...

🧪 [TEST] Имитация закрытия позиций для BTCUSDT
🔒 Закрыта пара позиций (Схождение спреда)
  ...
```

✅ Показывает **статистику**:

```
📊 СТАТИСТИКА ТОРГОВЛИ
Открытых позиций: 0
Закрытых сделок: 5
Прибыльных: 4
Убыточных: 1
Win Rate: 80.0%
Общая прибыль: $12.50
Общий убыток: $1.20
Чистая прибыль: $11.30
```

❌ **НЕ отправляет** реальные ордера на биржи

---

## 💰 Реальная торговля

### ⚠️ ВНИМАНИЕ!

Реальная торговля использует **РЕАЛЬНЫЕ ДЕНЬГИ**!

### Подготовка:

#### 1. Получи API ключи

**Binance:**
1. Перейди: https://www.binance.com/en/my/settings/api-management
2. Создай новый API ключ
3. Разрешения: **Futures Trading**
4. IP Whitelist (рекомендуется)

**MEXC:**
1. Перейди: https://www.mexc.com/user/openapi
2. Создай новый API ключ
3. Разрешения: **Futures Trading**

#### 2. Добавь ключи в .env

```bash
# Скопируй пример
cp .env.example .env

# Отредактируй .env
BINANCE_API_KEY=твой_binance_api_key
BINANCE_API_SECRET=твой_binance_secret

MEXC_API_KEY=твой_mexc_api_key
MEXC_API_SECRET=твой_mexc_secret
```

#### 3. Включи реальную торговлю

```json
{
  "trading": {
    "enabled": true,
    "testMode": false  // ⚠️ РЕАЛЬНЫЕ ДЕНЬГИ!
  }
}
```

#### 4. Запусти

```bash
npm start
```

Увидишь предупреждение:

```
⚠️  ТОРГОВЛЯ ВКЛЮЧЕНА! Режим: РЕАЛЬНЫЙ
🔴 ВНИМАНИЕ: Реальная торговля с реальными деньгами!
    Убедитесь, что API ключи настроены!
```

### Логи реальной торговли:

```
🔴 [REAL] Открытие реальных позиций для BTCUSDT...
✓ Открыта пара позиций #1
  ...

🔴 [REAL] Закрытие реальных позиций для BTCUSDT...
🔒 Закрыта пара позиций (Схождение спреда)
  ...
```

---

## 🛡️ Управление рисками

### 1. Размер позиции

Начни с малого:

```json
"positionSizeUSD": 50  // $50 на каждую биржу = $100 всего
```

По мере уверенности увеличивай.

### 2. Максимум позиций

Ограничь риск:

```json
"maxOpenPositions": 2  // Не более 2 пар одновременно
```

### 3. Таймаут

Не держи долго:

```json
"positionTimeoutSeconds": 1800  // 30 минут максимум
```

### 4. Плечо

Для арбитража рекомендуется **минимальное плечо**:

```json
"leverage": 1  // Без плеча = меньше риска ликвидации
```

### 5. Режим маржи

**Cross margin** (рекомендуется):
- Использует весь баланс как обеспечение
- Меньше риск ликвидации

```json
"marginMode": "cross"
```

**Isolated margin**:
- Использует только выделенную маржу
- Выше риск ликвидации
- Ограничивает убытки

```json
"marginMode": "isolated"
```

---

## 📈 Оптимизация настроек

### Агрессивная стратегия

Больше сделок, больше риск:

```json
{
  "arbitrage": {
    "minSpreadPercent": 0.3  // Ниже порог
  },
  "trading": {
    "positionSizeUSD": 200,
    "maxOpenPositions": 5,
    "leverage": 2,
    "positionTimeoutSeconds": 1800,
    "minProfitToClosePercent": 0.2
  }
}
```

### Консервативная стратегия

Меньше сделок, меньше риск:

```json
{
  "arbitrage": {
    "minSpreadPercent": 1.0  // Выше порог
  },
  "trading": {
    "positionSizeUSD": 50,
    "maxOpenPositions": 2,
    "leverage": 1,
    "positionTimeoutSeconds": 3600,
    "minProfitToClosePercent": 0.5
  }
}
```

---

## ❓ FAQ

### Q: Бот не находит спреды, почему?

**A:** Возможные причины:
1. `minSpreadPercent` слишком высокий — попробуй 0.3-0.5%
2. Низкая волатильность рынка
3. Мало общих пар между биржами

### Q: Как часто бот проверяет таймауты?

**A:** Каждые 10 секунд.

### Q: Можно ли закрыть позиции вручную?

**A:** Сейчас нет, но можно добавить. Либо останови бота (Ctrl+C) — все позиции останутся открытыми на биржах.

### Q: Что делать если бот упал с открытыми позициями?

**A:**
1. Зайди на биржи вручную
2. Закрой позиции через веб-интерфейс
3. Или перезапусти бота — он продолжит мониторинг

### Q: Учитываются ли комиссии?

**A:** Да! Бот вычитает:
- Комиссии при открытии (taker fee)
- Комиссии при закрытии (taker fee)
- Показывает **чистую прибыль** после всех комиссий

### Q: Какой минимальный спред выгоден?

**A:**
- Binance taker: 0.04%
- MEXC taker: 0.03%
- Итого комиссии: ~0.07%
- **Минимальный выгодный спред: 0.1-0.2%**

Но рекомендуется ≥0.5% для комфортной прибыли.

---

## 🔍 Мониторинг и логи

### Открытие позиции:

```
✓ Открыта пара позиций #1
  Symbol: BTCUSDT
  LONG:  BINANCE @ 42000.00
  SHORT: MEXC @ 42570.00
  Спред: 1.36%
  Ожидаемая прибыль: 1.29%
  Закрытие через: 3600s
```

### Закрытие позиции:

```
🔒 Закрыта пара позиций (Схождение спреда)
  Symbol: BTCUSDT
  Время удержания: 120s
  Прибыль: 0.85% ($1.70)
  Открытых позиций: 0
```

### Финальная статистика:

```
📊 СТАТИСТИКА ТОРГОВЛИ
════════════════════════════════════════
Открытых позиций: 0
Закрытых сделок: 10
Прибыльных: 8
Убыточных: 2
Win Rate: 80.0%
Общая прибыль: $25.40
Общий убыток: $3.20
Чистая прибыль: $22.20
════════════════════════════════════════
```

---

## 🚨 Предупреждения

### ⚠️ Риски:

1. **Проскальзывание** — реальная цена может отличаться
2. **Комиссии за вывод** — перевод между биржами стоит денег
3. **Задержки API** — спред может исчезнуть
4. **Ликвидация** — при использовании плеча
5. **Волатильность** — резкие движения цены

### 🛡️ Рекомендации:

- ✅ Начни с тестового режима
- ✅ Используй малые суммы
- ✅ Используй минимальное плечо (1x)
- ✅ Мониторь открытые позиции
- ✅ Проверяй баланс на биржах
- ❌ Не используй все средства
- ❌ Не оставляй без присмотра

---

## 📚 Дополнительно

### Структура Position:

```typescript
interface Position {
  id: string;              // UUID
  symbol: string;          // "BTCUSDT"
  exchange: 'binance' | 'mexc';
  side: 'LONG' | 'SHORT';
  entryPrice: number;
  exitPrice?: number;
  quantity: number;
  sizeUSD: number;
  leverage: number;
  status: 'OPEN' | 'CLOSED' | 'TIMEOUT_CLOSED';
  openTime: number;
  closeTime?: number;
  pnl?: number;
  pnlPercent?: number;
}
```

### Структура PositionPair:

```typescript
interface PositionPair {
  id: string;
  symbol: string;
  longPosition: Position;
  shortPosition: Position;
  openSpread: number;
  currentSpread?: number;
  expectedProfit: number;
  actualProfit?: number;
  status: 'OPEN' | 'CLOSED' | 'TIMEOUT_CLOSED';
  openTime: number;
  closeTime?: number;
  timeoutAt: number;
}
```

---

## 🎓 Заключение

Торговый модуль полностью функционален и готов к использованию!

**Рекомендуемый workflow:**

1. Тестируй в `testMode: true` несколько дней
2. Оцени win rate и прибыльность
3. Настрой параметры под свой риск-профиль
4. Переключись на реальную торговлю с малыми суммами
5. Постепенно увеличивай размер позиций

**Удачи в арбитраже! 🚀**

---

*Отказ от ответственности: Торговля криптовалютой несет высокие риски. Автор не несет ответственности за финансовые потери.*
