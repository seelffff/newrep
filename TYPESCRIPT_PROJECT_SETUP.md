# 📘 Полное руководство: Создание идеального TypeScript проекта с нуля

## 🎯 Содержание
1. [Инициализация проекта](#шаг-1-инициализация-проекта)
2. [Установка зависимостей](#шаг-2-установка-зависимостей)
3. [Настройка TypeScript](#шаг-3-настройка-typescript)
4. [Настройка package.json](#шаг-4-настройка-packagejson)
5. [Создание структуры](#шаг-5-создание-структуры)
6. [Первый запуск](#шаг-6-первый-запуск)

---

## Шаг 1: Инициализация проекта

### Команды:
```bash
mkdir my-awesome-project
cd my-awesome-project
git init
npm init -y
```

### Подробное объяснение:

#### `mkdir my-awesome-project`
**Что делает:** Создает новую папку для проекта

**Аналоги:**
- В Windows: `md my-awesome-project`
- Через Finder/Explorer: создать папку вручную

**Когда использовать:** Всегда при старте нового проекта

---

#### `git init`
**Что делает:** Инициализирует пустой Git репозиторий в текущей папке

**Что происходит под капотом:**
- Создается скрытая папка `.git/`
- Настраивается локальное хранилище для версионирования

**Аналоги:**
- GitHub Desktop: "Create new repository"
- VS Code: Source Control → Initialize Repository

**Когда использовать:**
- ✅ Сразу при создании проекта (best practice)
- ❌ Не нужно, если клонируешь существующий репозиторий (`git clone`)

**Можно ли пропустить?**
Да, но крайне не рекомендуется. Git — это страховка от потери кода.

---

#### `npm init -y`
**Что делает:** Создает файл `package.json` с настройками по умолчанию

**Флаг `-y` (или `--yes`):**
- Автоматически отвечает "yes" на все вопросы
- Создает package.json без интерактивного опроса

**Без флага `-y`:**
```bash
npm init
```
Будет задавать вопросы:
- package name? (my-awesome-project)
- version? (1.0.0)
- description?
- entry point? (index.js)
- test command?
- git repository?
- keywords?
- author?
- license? (ISC)

**Аналоги:**
- `yarn init` — то же самое для Yarn
- `pnpm init` — то же самое для pnpm
- `bun init` — то же самое для Bun

**Что лучше использовать?**
| Менеджер | Скорость | Популярность | Когда использовать |
|----------|----------|--------------|-------------------|
| **npm** | Средняя | Очень высокая | По умолчанию, максимальная совместимость |
| **yarn** | Быстрая | Высокая | Большие проекты, monorepo |
| **pnpm** | Очень быстрая | Растущая | Экономия диска, строгость |
| **bun** | Самая быстрая | Новая | Экспериментальные проекты |

**Рекомендация для начинающих:** используй **npm** — он идет с Node.js из коробки.

---

## Шаг 2: Установка зависимостей

### Команда:
```bash
npm install -D typescript @types/node tsx nodemon ts-node
```

### Подробная расшифровка:

#### Флаг `-D` (или `--save-dev`)
**Что означает:** Устанавливает пакеты как **dev dependencies** (зависимости разработки)

**Разница между dependencies и devDependencies:**

```json
{
  "dependencies": {
    "express": "^4.18.0"  // Нужен в продакшене
  },
  "devDependencies": {
    "typescript": "^5.0.0"  // Нужен только при разработке
  }
}
```

**Аналоги флага:**
- `-D` = `--save-dev` (короткая форма)
- `-S` = `--save` (обычные зависимости, используется по умолчанию)
- `-g` = `--global` (глобальная установка в систему)

**Примеры:**
```bash
npm install -D eslint        # DevDependency
npm install express          # Обычная dependency
npm install -g typescript    # Глобальная установка
```

---

### Устанавливаемые пакеты:

#### 1. `typescript`
**Что это:** Компилятор TypeScript — превращает `.ts` файлы в `.js`

**Зачем нужен:**
- Проверяет типы
- Компилирует современный TS в JS, который понимает Node.js

**Команды, которые он дает:**
- `tsc` — компилятор
- `tsc --init` — создать tsconfig.json
- `tsc --noEmit` — проверить типы без компиляции

**Альтернативы:** Нет полноценных аналогов

---

#### 2. `@types/node`
**Что это:** Типы для Node.js API

**Зачем нужен:** Без него TypeScript не знает типы для `console.log`, `fs`, `http` и других Node.js модулей

**Пример проблемы без @types/node:**
```typescript
import fs from 'fs';  // ❌ Error: Cannot find module 'fs'
```

**С @types/node:**
```typescript
import fs from 'fs';  // ✅ Работает, есть автодополнение
```

**Альтернативы:** Нет, это официальный пакет

---

#### 3. `tsx`
**Что это:** Сверхбыстрый способ запуска TypeScript БЕЗ компиляции

**Как работает:**
- Использует esbuild под капотом (очень быстрый)
- Запускает `.ts` файлы напрямую в Node.js

**Когда использовать:** В режиме разработки (`npm run dev`)

**Альтернативы:**
| Пакет | Скорость | Особенности |
|-------|----------|-------------|
| **tsx** | ⚡⚡⚡ Очень быстрая | Современный, активная поддержка |
| **ts-node** | ⚡⚡ Средняя | Старый стандарт, больше возможностей |
| **ts-node-dev** | ⚡⚡ Средняя | ts-node + автоперезагрузка |
| **bun** | ⚡⚡⚡⚡ Самая быстрая | Экспериментальный |

**Рекомендация:** Используй **tsx** для новых проектов.

**Команда запуска:**
```bash
tsx ./src/main.ts
```

---

#### 4. `nodemon`
**Что это:** Утилита для автоматической перезагрузки приложения при изменении файлов

**Как работает:**
1. Следит за файлами в папке `src/`
2. При изменении — убивает процесс и перезапускает

**Когда использовать:** В режиме разработки

**Альтернативы:**
| Инструмент | Когда использовать |
|------------|-------------------|
| **nodemon** | Универсальный вариант, работает с любыми ком��ндами |
| **ts-node-dev** | Специально для TypeScript, но медленнее tsx |
| **--watch** флаг Node.js | Node.js 18+, встроенная функция |
| **bun --watch** | Если используешь Bun |

**Пример с встроенным --watch (Node.js 18+):**
```bash
node --watch ./build/main.js
```

**Почему мы используем nodemon:**
- Работает на всех версиях Node.js
- Гибкая настройка через `nodemon.json`
- Стабильность и надежность

**Команда:**
```bash
nodemon --exec tsx ./src/main.ts
```

Читается как: "Nodemon, запускай команду `tsx ./src/main.ts` и перезапускай при изменениях"

---

#### 5. `ts-node`
**Что это:** Классический способ запуска TypeScript в Node.js

**Зачем устанавливаем:** На случай, если tsx не подойдет (редко)

**Отличия от tsx:**
- Медленнее запускается
- Больше возможностей для настройки
- Поддержка старых проектов

**Когда использовать ts-node вместо tsx:**
- Нужны специфичные настройки компиляции
- Работа со старым проектом, где уже используется ts-node
- Проблемы совместимости с tsx

**Команда:**
```bash
ts-node ./src/main.ts
```

**Рекомендация:** Оставь tsx по умолчанию, ts-node как запасной вариант.

---

## Шаг 3: Настройка TypeScript

### Команда:
```bash
touch tsconfig.json
```

**Альтернативы создания файла:**
- Windows: `type nul > tsconfig.json`
- Автоматически: `npx tsc --init` (создаст с комментариями)
- VS Code: правый клик → New File

---

### Содержимое tsconfig.json:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "node",
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "outDir": "./build",
    "rootDir": "./src",
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "sourceMap": true,
    "removeComments": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "build"]
}
```

### Подробная расшифровка каждой опции:

#### `"target": "ES2020"`
**Что это:** В какую версию JavaScript компилировать код

**Варианты:**
- `ES5` — старые браузеры (IE11)
- `ES2015` (ES6) — минимум для современных браузеров
- `ES2020` — Node.js 14+
- `ES2022` — Node.js 18+
- `ESNext` — самые новые фичи

**Что выбрать:**
- Node.js проект → `ES2020` или `ES2022`
- Браузер (современные) → `ES2020`
- Браузер (старые) → `ES2015`

---

#### `"module": "ESNext"`
**Что это:** Какую систему модулей использовать

**Варианты:**
- `CommonJS` — старый формат (`require/module.exports`)
- `ESNext` — современный формат (`import/export`)
- `UMD` — универсальный (браузер + Node.js)

**Что выбрать:**
- ✅ `ESNext` — если в package.json есть `"type": "module"`
- `CommonJS` — для старых проектов

**Важно:** Если используешь `ESNext`, обязательно добавь в импорты `.js`:
```typescript
import { foo } from './module.js'  // ✅ Правильно
import { foo } from './module'      // ❌ Не сработает
```

---

#### `"moduleResolution": "node"`
**Что это:** Как TypeScript ищет модули

**Варианты:**
- `node` — как Node.js (стандарт)
- `node16` / `nodenext` — Node.js с ES модулями
- `bundler` — для Webpack/Vite

**Рекомендация:** Используй `node` — универсальный вариант.

---

#### `"esModuleInterop": true`
**Что это:** Позволяет импортировать CommonJS модули в ES модулях

**Пример:**
```typescript
// С esModuleInterop: true
import express from 'express'  // ✅ Работает

// Без esModuleInterop
import * as express from 'express'  // ❌ Нужно так
```

**Рекомендация:** Всегда включай (`true`)

---

#### `"allowSyntheticDefaultImports": true`
**Что это:** Разрешает импорт по умолчанию для модулей без default export

**Пример:**
```typescript
import React from 'react'  // Работает благодаря этой опции
```

**Рекомендация:** Включай вместе с `esModuleInterop`

---

#### `"outDir": "./build"`
**Что это:** Куда складывать скомпилированный JavaScript

**Альтернативы:**
- `./build` — традиционное название
- `./dist` — тоже популярно
- `./out` — реже используется

**Рекомендация:** Используй `./build` или `./dist`

---

#### `"rootDir": "./src"`
**Что это:** Откуда брать исходный TypeScript код

**Зачем нужно:** Сохраняет структуру папок при компиляции

**Пример:**
```
src/
  utils/
    helper.ts
  main.ts

↓ После компиляции с rootDir: "./src" ↓

build/
  utils/
    helper.js
  main.js
```

---

#### `"strict": true`
**Что это:** Мастер-переключатель всех строгих проверок

**Включает:**
- `noImplicitAny`
- `strictNullChecks`
- `strictFunctionTypes`
- `strictBindCallApply`
- `strictPropertyInitialization`
- и другие

**Рекомендация:** ✅ ВСЕГДА включай! Это основа TypeScript.

---

#### `"noImplicitAny": true`
**Что это:** Запрещает неявный тип `any`

**Пример:**
```typescript
// ❌ С noImplicitAny: true
function log(message) {  // Error: параметр имеет тип 'any'
  console.log(message);
}

// ✅ Правильно
function log(message: string) {
  console.log(message);
}
```

---

#### `"strictNullChecks": true`
**Что это:** Запрещает присваивать `null` и `undefined` без явного указания

**Пример:**
```typescript
// ❌ С strictNullChecks: true
let name: string = null;  // Error!

// ✅ Правильно
let name: string | null = null;
```

---

#### `"noUnusedLocals": true`
**Что это:** Ошибка при неиспользуемых переменных

**Пример:**
```typescript
function calculate() {
  const result = 5 + 5;  // ❌ Error: 'result' is declared but never used
  const answer = 10;     // ✅ OK, используется ниже
  return answer;
}
```

**Когда полезно:** Помогает держать код чистым

---

#### `"noUnusedParameters": true`
**Что это:** Ошибка при неиспользуемых параметрах функции

**Пример:**
```typescript
// ❌ Error
function greet(name: string, age: number) {
  console.log(name);  // age не используется!
}

// ✅ Если параметр не нужен, используй _
function greet(name: string, _age: number) {
  console.log(name);
}
```

---

#### `"noImplicitReturns": true`
**Что это:** Все ветки функции должны что-то возвращать

**Пример:**
```typescript
// ❌ Error
function getDiscount(age: number): number {
  if (age > 65) {
    return 10;
  }
  // Забыли return для других случаев!
}

// ✅ Правильно
function getDiscount(age: number): number {
  if (age > 65) {
    return 10;
  }
  return 0;  // Обязательно
}
```

---

#### `"sourceMap": true`
**Что это:** Создает `.map` файлы для отладки

**Зачем:** Позволяет дебажить TypeScript код, а не скомпилированный JavaScript

**Когда отключать:** В финальной production сборке (для экономии места)

---

#### `"removeComments": true`
**Что это:** Удаляет комментарии из скомпилированного JS

**Плюсы:** Меньше размер файлов

**Минусы:** Теряются JSDoc комментарии

**Рекомендация:** Включай для production

---

#### `"skipLibCheck": true`
**Что это:** Не проверять типы в файлах `node_modules/**/*.d.ts`

**Зачем:** Ускоряет компиляцию

**Рекомендация:** Всегда включай

---

#### `"forceConsistentCasingInFileNames": true`
**Что это:** Проверяет регистр букв в именах файлов

**Пример:**
```typescript
// Файл называется User.ts

import { User } from './user'   // ❌ Error (регистр не совпадает)
import { User } from './User'   // ✅ OK
```

**Зачем:** Избегает проблем на case-sensitive системах (Linux)

---

## Шаг 4: Настройка package.json

### Добавление скриптов:

```json
"scripts": {
  "dev": "nodemon --exec tsx ./src/main.ts",
  "build": "npm run clean && tsc",
  "build:prod": "npm run clean && tsc && npm run build:check",
  "build:check": "node ./build/main.js",
  "clean": "rm -rf build",
  "start": "node ./build/main.js",
  "typecheck": "tsc --noEmit"
}
```

### Подробная расшифровка каждого скрипта:

#### `"dev": "nodemon --exec tsx ./src/main.ts"`
**Когда использовать:** В процессе разработки

**Что происходит:**
1. nodemon следит за изменениями в `src/`
2. При изменении → запускает `tsx ./src/main.ts`
3. tsx компилирует и запускает TypeScript

**Альтернативные варианты:**
```json
// Вариант 1: С ts-node вместо tsx
"dev": "nodemon --exec ts-node ./src/main.ts"

// Вариант 2: Node.js 18+ встроенный watch
"dev": "node --watch --loader ts-node/esm ./src/main.ts"

// Вариант 3: С tsx и дополнительными флагами
"dev": "nodemon --exec tsx --no-cache ./src/main.ts"
```

**Флаги nodemon:**
- `--exec <command>` — какую команду выполнять
- `--watch <dir>` — какие папки смотреть (по умолчанию все)
- `--ext <extensions>` — какие расширения отслеживать
- `--ignore <pattern>` — что игнорировать

**Пример с дополнительными настройками:**
```json
"dev": "nodemon --watch src --ext ts,js --exec tsx ./src/main.ts"
```

---

#### `"build": "npm run clean && tsc"`
**Когда использовать:** Обычная сборка проекта

**Что происходит:**
1. `npm run clean` — удаляет старую папку `build/`
2. `&&` — оператор "И" (выполнить следующую команду, если первая успешна)
3. `tsc` — компилирует TypeScript в JavaScript

**Оператор `&&` vs `;`:**
```bash
# && — следующая команда ТОЛЬКО если предыдущая успешна
npm run clean && tsc

# ; — выполнить в любом случае
npm run clean; tsc
```

**Альтернативы:**
```json
// Без очистки
"build": "tsc"

// С проверкой перед сборкой
"build": "npm run typecheck && npm run clean && tsc"

// Параллельная сборка нескольких конфигов
"build": "tsc -p tsconfig.json && tsc -p tsconfig.build.json"
```

---

#### `"build:prod": "npm run clean && tsc && npm run build:check"`
**Когда использовать:** Перед деплоем в production

**Что происходит:**
1. Очистка `build/`
2. Компиляция
3. **Проверка запуска** — гарантирует, что собранный код работает!

**Почему это важно:**
- Ловит ошибки импортов (забыли `.js` в конце)
- Проверяет, что все зависимости на месте
- Убеждается, что код действительно запускается

---

#### `"build:check": "node ./build/main.js"`
**Когда использовать:** Автоматически через `build:prod` или вручную

**Что делает:** Пробный запуск собранного кода

**Расширенные варианты:**
```json
// С проверкой exit code
"build:check": "node ./build/main.js && echo 'Build OK!'"

// С таймаутом (если приложение долго запускается)
"build:check": "timeout 5 node ./build/main.js"
```

---

#### `"clean": "rm -rf build"`
**Когда использовать:** Перед новой сборкой

**Платформозависимость:**
```json
// Unix/Linux/macOS
"clean": "rm -rf build"

// Windows (с cross-env)
"clean": "rmdir /s /q build"

// Кроссплатформенное решение (требует rimraf)
"clean": "rimraf build"
```

**Установка rimraf (кроссплатформенная замена rm):**
```bash
npm install -D rimraf
```

Тогда:
```json
"clean": "rimraf build"
```

---

#### `"start": "node ./build/main.js"`
**Когда использовать:** В production (на сервере)

**Особенности:**
- Запускает УЖЕ скомпилированный JavaScript
- НЕ требует TypeScript на сервере
- Быстрый старт

**Альтернативы:**
```json
// С переменными окружения
"start": "NODE_ENV=production node ./build/main.js"

// С PM2 (процесс-менеджер для production)
"start": "pm2 start ./build/main.js"

// С флагами Node.js
"start": "node --max-old-space-size=4096 ./build/main.js"
```

---

#### `"typecheck": "tsc --noEmit"`
**Когда использовать:** Быстрая проверка типов без компиляции

**Флаг `--noEmit`:**
- Проверяет типы
- НЕ создает файлы JavaScript
- Очень быстро работает

**Зачем нужен отдельный скрипт:**
- Проверка типов перед коммитом
- CI/CD пайплайны
- Быстрая валидация при разработке

**Использование в CI:**
```yaml
# .github/workflows/ci.yml
- name: Type check
  run: npm run typecheck
```

---

## Шаг 5: Создание структуры

```bash
mkdir src
touch src/main.ts
touch .gitignore
touch README.md
```

### .gitignore содержимое:

```
# Зависимости
node_modules/

# Собранные файлы
build/
dist/

# Логи
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Переменные окружения
.env
.env.local
.env.*.local

# Системные файлы
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
```

---

## Шаг 6: Первый запуск

```bash
# Проверка типов
npm run typecheck

# Разработка
npm run dev

# Сборка
npm run build:prod
```

---

## 📊 Сравнительная таблица: Когда какую команду использовать

| Сценарий | Команда | Почему |
|----------|---------|--------|
| Пишешь код | `npm run dev` | Автоперезагрузка, быстро |
| Проверить типы | `npm run typecheck` | Без компиляции, моментально |
| Перед коммитом | `npm run typecheck` | Убедиться, что нет ошибок |
| Перед деплоем | `npm run build:prod` | Полная проверка + сборка |
| На сервере | `npm start` | Запуск готового кода |
| Очистить артефакты | `npm run clean` | Перед пересборкой |

---

## 🎯 Чек-лист готовности проекта

- [ ] `package.json` создан с правильными скриптами
- [ ] `tsconfig.json` настроен со `strict: true`
- [ ] `.gitignore` создан
- [ ] Установлены все зависимости (`node_modules/` существует)
- [ ] `npm run typecheck` проходит без ошибок
- [ ] `npm run dev` запускается
- [ ] `npm run build:prod` работает
- [ ] Первый Git коммит сделан

---

## 🚀 Дополнительные инструменты (опционально)

### ESLint — проверка качества кода

```bash
npm install -D eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin
```

**Что дает:**
- Находит потенциальные баги
- Форсирует единый стиль кода
- Предотвращает плохие практики

**Добавить в scripts:**
```json
"lint": "eslint src/**/*.ts",
"lint:fix": "eslint src/**/*.ts --fix"
```

---

### Prettier — форматирование кода

```bash
npm install -D prettier
```

**Создать `.prettierrc`:**
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5"
}
```

**Добавить в scripts:**
```json
"format": "prettier --write 'src/**/*.ts'",
"format:check": "prettier --check 'src/**/*.ts'"
```

---

### Husky — Git hooks

```bash
npm install -D husky
npx husky init
```

**Пример pre-commit hook:**
```bash
# .husky/pre-commit
npm run typecheck
npm run lint
```

**Зачем:** Автоматические проверки перед коммитом

---

## 📚 Итоговая структура проекта

```
my-awesome-project/
├── src/
│   └── main.ts
├── build/                 # Создается после npm run build
│   └── main.js
├── node_modules/          # Создается после npm install
├── .git/                  # Скрытая папка Git
├── .gitignore
├── package.json
├── package-lock.json
├── tsconfig.json
└── README.md
```

---

## 🎓 Важные правила профессиональной разработки

1. **Всегда используй TypeScript strict mode**
2. **Коммит build/ не нужен** — добавь в .gitignore
3. **node_modules/ никогда не коммитим**
4. **Проверяй типы перед коммитом** — `npm run typecheck`
5. **Тестируй production сборку** — `npm run build:prod`
6. **Используй ES модули** — современный стандарт
7. **Пиши типы явно** — не злоупотребляй `any`

---

Готово! 🎉 Теперь у тебя есть полное руководство для создания профессиональных TypeScript проектов.
